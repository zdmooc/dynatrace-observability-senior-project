# Instrumentation

- JVM / services : OneAgent auto-instrumentation + règles
- Logs : ingestion, parsing, corrélation
- Traces : distribué, propagation, sampling
- Intégrations : Kafka, DB, ingress, service mesh (si présent)
